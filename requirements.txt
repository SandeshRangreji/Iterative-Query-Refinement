# Minimal requirements.txt - dependencies auto-resolved

# Core packages directly used
datasets==3.4.1
sentence-transformers==3.4.1
bertopic==0.17.0
matplotlib==3.10.1
seaborn==0.13.2
plotly==6.0.1

# NLP packages directly used
nltk==3.9.1
rank-bm25==0.2.2
keybert==0.9.0

# Topic modeling
gensim==4.3.3
scipy<=1.13.1  # Required for gensim 4.3.3 compatibility (triu issue with 1.14+)

# GPU acceleration
cupy-cuda11x>=11.0.0